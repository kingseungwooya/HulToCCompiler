#include <stdio.h>
int main() {
	int _hul;
	int max0 = 10;
	int max1 = 2;
	int max2 = 3;
	for (int i0=0; i0 < max+0; i0++){
 		printf("input: ");
		scanf("%d", &_hul);
		for (int i1=0; i1 < max+1; i1++){
 			_hul++;
			for (int i2=0; i2 < max+2; i2++){
 				_hul++;
				}3
			}2
		printf("%d", _hul);
		}10
	return 0;
}
